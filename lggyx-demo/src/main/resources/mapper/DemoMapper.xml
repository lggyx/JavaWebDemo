<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lggyx.mapper.DemoMapper">
    <insert id="add">
        insert into demo(name, description, category, content, image, create_time, update_time)
        values (#{name}, #{description}, #{category}, #{content}, #{image}, #{createTime}, #{updateTime})
    </insert>
    <update id="update">
        update demo
        set name        = #{name},
            description = #{description},
            category    = #{category},
            content     = #{content},
            image       = #{image},
            update_time = #{updateTime}
        where id = #{id}
    </update>
    <delete id="delete">
        delete
        from demo
        where id = #{id}
    </delete>
    <delete id="deletes">
        delete
        from demo
        where id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
    <select id="selectById" resultType="com.lggyx.pojo.Demo">
        select *
        from demo
        where id = #{id}
    </select>
</mapper>
